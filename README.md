# Messaging-App
Multi-user chat built in C++ using Winsock sockets. The server handles concurrent users via multithreading, ensuring isolated message processing for each client. Focuses on TCP connection management, thread safety, and real-time data relay between multiple active sessions.
